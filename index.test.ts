import { ZapierActions } from "./index";

describe("zapier-actions-typescript-sdk", () => {
    it("initialize client", async () => {
        const zapieractions = new ZapierActions({
        });
    });
});
